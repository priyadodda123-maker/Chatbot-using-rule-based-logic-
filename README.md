# Chatbot-using-rule-based-logic-
A rule-based chatbot is a simple AI program that responds to user inputs using predefined rules. It does not learn from data; instead, it uses if-else conditions or dictionaries to generate responses.
